import { StyleSheet } from 'react-native'

const header = StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: 'center',
  },
  text: {
    fontSize: 38,
    fontWeight: 'bold',
  },
})

export default header
